# Contributing

New checks must be deterministic, must not emit credential values, and must include both a positive test and a false-positive counterexample. Run the standard-library test suite before opening a pull request.
